# angel-task-mgmt

## TypeORM

### Migration

1. Create migration file.
    ```shell
   $ npm run migration:generate <MIGRATION_FILE_NAME>
   ```

2. Run migration.
   ```shell
   $ npm run migration:run
   ```